# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/trips_schema
spring.datasource.username=root
spring.datasource.password=Iamhungry123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# the line above was added to stop web server from starting so that we could test the database

# gRPC server configuration
# grpc.server.port=5003

# Application HTTP server port
server.port=8080

# Logging Configuration
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG
